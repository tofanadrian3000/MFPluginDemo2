
trigger:
- master

pool: 'Win8.1'
#pool: 'Win2016'
#pool: 'Default'
#pool: 'AzureVms'

steps:

# Default configuration (on an absolute path to the scenario)
#   -> default parameters are not required
- task: ExecuteLoadRunnerTestsRelease@1
  inputs:
    SourcePath: 'C:\Scenarios-Multiple'

- task: ExecuteLoadRunnerTestsRelease@1
  inputs:
    SourcePath: 'C:\Scenarios-Failing'
    ResultsDirectory: 'C:\LR'